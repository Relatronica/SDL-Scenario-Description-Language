/**
 * SDL Citizen Lab — Scenario Library
 *
 * All scenarios are now SDL native: parsed and rendered directly from .sdl files.
 */

export type { ScenarioMeta, SliderDef, VariableDisplay, ScenarioCategory } from './types';
export { CATEGORY_LABELS, CATEGORY_COLORS } from './types';
export { SDL_NATIVE_SCENARIOS, type SdlNativeScenario } from './sdl-native';
